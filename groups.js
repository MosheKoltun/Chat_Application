//========================================================
function Group(name, users) {
    this.name = name;
    this.users = users || [];
}
//========================================================
Group.prototype.addUser = function () {
    this.name = name;
}